[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --cov=gemini_chat
    --cov-report=term-missing
    --cov-report=html
    --asyncio-mode=auto
